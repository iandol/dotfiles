\documentclass{$if(my-doc-class)$$my-doc-class$$else$minimal$endif$}
\usepackage{ifxetex,ifluatex}
\ifxetex
	\usepackage{mathspec}
\fi
$if(mainfont)$
	\setmainfont[$for(mainfontoptions)$$mainfontoptions$$sep$,$endfor$]{$mainfont$}
$endif$
\begin{document}
$greeting$
$body$
Some unicode: — … ± 安全与电子商务 
\end{document}
#!/usr/bin/env zsh
# This script converts the BibTeX auto-generated by Bookends and makes JSON
# that is more easily consumed by Pandoc


if [[ $# -eq 0 ]]; then
	dn=$HOME/Library/CloudStorage/Dropbox/Papers/References
	[[ -f "🌊Core.bib" ]] && mv 🌊Core.bib Core.bib
	filename="Core.bib"
else
	dn=$(dirname $1)
	filename=$(basename $1)
fi
cd $dn
mydir=$(pwd)
printf "\n\n============================================\n"
printf "---> Processing Bibliography file $filename in $mydir @ $(date)...\n"
if [[ ! -s $filename ]]; then # make sure file exists
	printf "\nCannot find $filename\n"
	return -1
fi
printf '---> Citeproc generating JSON from BIB file...\n' 
/usr/local/bin/pandoc --verbose -f bibtex -t csljson $filename > Temp.json
if [[ ! -s Temp.json ]]; then
	printf "\tPandoc failed with $filename\n"
	return -1
fi
printf '---> Processing title case...\n'
/Users/ian/bin/fixCase.rb Temp.json
printf "---> Finshed processing title case...\n"
if [[ -s Temp.json ]]; then
	printf '---> Minifying JSON...\n'
	/usr/local/bin/jq -Sc '. | del(.[]."abstract",.[]."keyword",.[]."publisher-place")' < Temp.json > Core.json
else
	printf '---> ERROR: Temp.json does not exist or is empty\n'
fi
[[ -s Temp.json ]] && rm Temp.json
printf '===>>> ...Finished!\n'